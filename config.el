(require 'optimizations) ; Load Emacs optimizations config file
(require 'core-config) ; Load the core configurations file
(require 'editor) ;; Editor settings config file
(require 'buffer-move) ;; Move Buffers accross emacs
(require 'shells) ;; Terminal Setup
(require 'eshell-prompt) ;; Eshell prompt config
(require 'evil-mode-config) ;;  Evil mode configurations file
(require 'ui-config) ;; UI configuration for emacs
(require 'dashboard-config) ;; Dashboard and modeline config for emacs
(require 'popups) ;; Popper configuration for emacs buffers
(require 'projects) ;; Project configuration
(require 'workspace) ;; Perspective.el config
(require 'minibuffer-ui) ;; Minibuffer UI configurations
(require 'consult-project-extra) ;; Consult integration for project.el
;; (require 'setup-corfu) ;; Corfu Configuration
;; (require 'setup-cape) ;; Cape Configuration
(require 'setup-orderless) ;; Orderless Configuration
(require 'setup-completions-default) ;; Builtin completions
;; (require 'tabnine-config) ;; Tabnine inline AI completion system
(require 'setup-yas) ;; Yasnippet configurations
(require 'consult-yasnippet) ;; Consult integration for Yasnippet
(require 'setup-git) ;; Git Configuration
(require 'setup-gptel) ;; GPTEL config
(require 'setup-calc) ;; Emacs CALC config
(require 'setup-dired) ;; Dired config
(require 'setup-treesit) ;; Tressiter Configuration
(require 'setup-eglot) ;; Eglot Setup
(require 'setup-doc) ;; PDF Tools
(require 'setup-tex) ;; LaTeX setup
(require 'setup-md) ;; Markdown Setup
(require 'setup-org) ;; Org mode Configurations
(require 'setup-calendar) ;; Calendar Configurations
