# -*- mode: snippet -*-
# name: dataset
# key: dataset
# --
@dataset{ ${title}
  title        = {${title}},
  editor       = {${editor}},
  year         = {${year}}${,
  author       = {${author}}}${,
  volume       = {${volume}}}${,
  version      = {${version}}}${,
  publisher    = {${publisher}}}${,
  organization = {${organization}}}${,
  note         = {${note}}}

}
